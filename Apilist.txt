Product controller

Products
Get all Products
Add a product 
Get one Product
Filter product
Rate a product

Add items to the cart 
remove items from the cart 
get items of the cart

Usercontroller
Signup - (Email,Name,Password,Type of User(seller,customer))
Signin - (Email,Password)


db.products.aggregate([
    //1. Create documents for rating.
    {
        $unwind:$rating
    },
    //2.Group rating per product and get average.
    {
        $group:{
            _id:"name"
            averageRating:{$avg:"$ratings:rating}
        }
    }
])

db.products.aggregate([
    //1.Project name of products, and the countOfRating
    {
        $project:{name:1, countOfRating:{
            $cond:{if:{$isArray:"ratings"} ,
            then:{
            $size:"ratings"},else:0}
    }}}
    //2.Sort the collection.
    {
        $sort:{countOfRating:-1}
    },
    //3.Limit to just 1 item in the result.
    {
        $limit:1
    }

])